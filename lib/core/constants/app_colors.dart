import 'package:flutter/material.dart';

class AppColors {
  // سفید با شفافیت 20% (#FFFFFF33)
  static const Color whiteOverlay = Color(0x33FFFFFF);

  // گرادیانت ها
  static const Color gradientStart = Color(0xFF5C609B); // معادل #5C609B
  static const Color gradientEnd   = Color(0xFF767CB2); // معادل #767CB2

  // گرادیانت دیگر (مثلا CSS linear-gradient 180deg #B6BDD0 -> #AEB4CA)
  static const Color gradient180Start = Color(0xFFB6BDD0);
  static const Color gradient180End   = Color(0xFFAEB4CA);

  // سایر رنگ های ثابت پروژه را اینجا اضافه کن
}
